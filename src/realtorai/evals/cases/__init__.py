"""Held-out cases, committed as Python literals.

`git ls-files data/` is empty by design — this repo processes real client
paperwork and everything under `data/` is gitignored. Eval cases are therefore
source, not data files, and every one is a fictionalised derivation of real
inbox traffic: names, addresses, and MLS numbers are invented, message *shapes*
are not.
"""
